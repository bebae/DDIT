<?xml version="1.0" encoding="UTF-8"?>
<!-- 이 문서는 mybatis에서 처리할 SQL문을 작성하는 문서입니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="member">
    <insert id="insertMember" parameterType="MemberVo">
        INSERT INTO mymember(mem_id, mem_pass, mem_name, mem_tel, mem_addr)
        VALUES(#{mem_id}, #{mem_pass}, #{mem_name}, #{mem_tel}, #{mem_addr})
    </insert>
    <delete id="deleteMember" parameterType="String">
        DELETE FROM mymember WHERE mem_id = #{mem_id}
    </delete>
    <update id="updateMember" parameterType="MemberVo">
        UPDATE mymember SET
            mem_name = #{mem_name},
            mem_tel = #{mem_tel},
            mem_addr = #{mem_addr}
        WHERE mem_id = #{mem_id}
    </update>
    <select id="getAllMember" resultType="MemberVo">
        SELECT mem_id, mem_name, mem_addr, mem_tel FROM mymember
    </select>
    <select id="getMemberCount" parameterType="MemberVo" resultType="Int">
        select count(*) from mymember where mem_id = #{mem_id}
    </select>

    <!--
        Map을 파라미터로 받아서 처리할 때는 변수명 역할을 Map의 키값으로 한다. (넘겨준 Map의 키값 = memid, field, data)
        파라미터로 가져온 값을 컬럼명 등의 자리에서 사용할 경우 달러($) 기호를 사용해서 나타낸다.
        예) ${변수명}, ${key값}
     -->
    <update id="updateDetail" parameterType="Map">
        UPDATE mymember SET ${field} = #{data}
            WHERE mem_id = #{memid}
    </update>

</mapper>