<?xml version="1.0" encoding="UTF-8"?>
<!-- 이 문서는 mybatis에서 처리할 SQL문을 작성하는 문서입니다. -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="board">
    <select id="getBoardList" resultType="BoardVo">
        SELECT board_no, board_title, board_writer, board_date, board_cnt, board_content FROM jdbc_board ORDER BY board_no DESC
    </select>
    <insert id="insertBoard" parameterType="BoardVo">
        INSERT INTO jdbc_board (board_no, board_title, board_writer, board_date, board_content) VALUES (board_seq.nextVal, #{board_title}, #{board_writer}, sysdate, #{board_content})
    </insert>
    <update id="updateBoard" parameterType="BoardVo">
        UPDATE jdbc_board SET board_title = #{board_title}, board_content = #{board_content} WHERE board_no = #{board_no}
    </update>
    <delete id="deleteBoard" parameterType="BoardVo">
        DELETE FROM jdbc_board WHERE board_no = #{board_no}
    </delete>
    <select id="getBoardByNo" parameterType="Int" resultType="BoardVo">
        SELECT * FROM jdbc_board WHERE board_no = #{board_no}
    </select>
    <select id="searchBoard" parameterType="String" resultType="BoardVo">
        SELECT * FROM jdbc_board WHERE board_title LIKE #{title}
    </select>
    <update id="increaseBoardCount" parameterType="Int">
        UPDATE jdbc_board SET board_cnt = board_cnt + 1 WHERE board_no = #{board_no}
    </update>

</mapper>